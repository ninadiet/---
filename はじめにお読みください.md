# キットまるごと最新化パック 2026-08-03（ドラッグ＆ドロップだけ・5分）

**いつ受け取ったキットでも、これ1つで最新になります。**
過去の更新パックを当てていない方も、当てた方も、このZIPだけでOKです。

⚠️ この更新は「Claudeに貼って適用」だと、キットの安全保護で止まります（「お応えできません」と出ます）。
**いつもの更新と同じ「ファイルをドラッグ＆ドロップ」**でお願いします。

## ⚠️ 当てる場所

**ホグワーツのキットのフォルダ**に当ててください（別キットのフォルダには当てないでください）。

## 手順

1. このZIPを**解凍**する
2. Claude Code で **ホグワーツのキットのフォルダを開く**
3. 解凍フォルダ直下にある **`.github` / `scripts` / `docs` / `.claude` の4フォルダと、
   その並びにある .md ファイルなど** を、まとめてドラッグ＆ドロップして**上書き**する
   - ※ **Claudeに「編集して」と頼まず、自分でドラッグ**します
4. 解凍フォルダの **`そのまま上書き_operationフォルダ`** を開き、
   中の **`operation` フォルダ**も同じようにドラッグ＆ドロップして上書きする
5. チャットに「**変更を全部 git add して commit して push して**」と入力

これで完了です。次回の自動実行から反映されます。

## ✅ あなたの設定・データは消えません

このZIPには、**あなたが記入・蓄積するファイルを1つも入れていません。**
入っていないので、上書きしようがない作りです。

| 触らないもの | 中身 |
|---|---|
| `operation/knowledge/kb_sys_ref_v001.md` | あなたの声の定義 |
| `operation/config/research_config.json` | ジャンル・収集キーワード |
| `operation/config/agent_names.json` | エージェントの名前 |
| `operation/weekly/` | 実績記録 |
| `operation/knowledge/low_er_posts.md` | 反省サンプル（自動） |
| `.env` ／ `operation/auth/` ／ `credentials/` | 認証情報 |

## 📁 「任意_テーマを自分で編集していない方だけ」フォルダ

`operation/themes/theme001.md` の差し替えです。**当てるかは任意**です。

- テーマを自分で書き換えたことが**ない**方 → 中の `operation` をドラッグして上書き
- 自分で編集した方 → **当てないでください**（内容が消えます）

## 🧹 古いファイルの掃除（昔からお使いの方だけ・1分）

昔のキットに入っていた**今は使わない仕組み**は、上書きでは消えません。
キットのフォルダに以下があったら、**エクスプローラーで削除**してください（無ければ何もしなくてOK）。

- `.github/workflows/monthly-token-discord.yml` （毎月のキー配布・廃止済み。残っていると毎月1日に無駄に動きます）
- `updates/` フォルダ （旧・更新パック置き場。廃止済み）
- `operation/auth/` の中の `access_～.key` ファイル （月次キー・廃止済み。今の認証はDiscord在籍確認だけです）

※ 削除もドラッグと同じく**ご自分の操作**でお願いします（Claudeに頼むと保護で止まります）。

## もし「お応えできません」と出たら

保護が正常に効いている合図です。Claudeに"編集"を頼むのをやめて、
手順3・4の"自分でドラッグ＆ドロップ"に変えれば通ります。
うまくいかない時は、コミュニティ管理者が一緒に対応します🙆
